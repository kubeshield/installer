apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "identity-server.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "identity-server.labels" . | nindent 4 }}
imagePullSecrets:
  {{- range $x := .Values.imagePullSecrets }}
  - name: "{{ $x }}"
  {{- end }}
